
import { occurenceMessage } from 'deck/helpers/occurence-message';
import { module, test } from 'qunit';

module('Unit | Helper | occurence message');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = occurenceMessage([42]);
  assert.ok(result);
});

