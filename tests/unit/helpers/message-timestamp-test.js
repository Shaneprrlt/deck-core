
import { messageTimestamp } from 'deck/helpers/message-timestamp';
import { module, test } from 'qunit';

module('Unit | Helper | message timestamp');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = messageTimestamp([42]);
  assert.ok(result);
});

