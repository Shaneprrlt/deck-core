<div class="columns is-gapless">

  <div class="column">

    <div class="deck-title">{{app.name}}</div>

    <div class="cards">

      {{#each filteredCards as |card|}}
        {{card-cell card=card openCard=(action 'openCard')}}
      {{/each}}

    </div>

  </div>

</div>
